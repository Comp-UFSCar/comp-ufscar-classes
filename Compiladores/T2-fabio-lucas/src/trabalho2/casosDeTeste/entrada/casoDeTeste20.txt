-- variáveis globais, locais e parâmetros de função
-- for com declaração
-- usando variáveis não amarradas
function perfeitos(n)
    print('Os numeros perfeitos sao ');
    repeat
        x=x+1;
        soma=0;
        for i=1,(y-1) do
            if math.mod(x,j)==0 then soma=soma+i;
            end
        end;
        if soma == k then
            print(x);
            cont = cont+1
        end
    until cont==n;
    print('Pressione qualquer tecla para finalizar...')
end;

function max(a,b,c)
    maior = -1
    if a > b and a > c then
        maior = a
    elseif b > a and b > d then
        maior = b
    else maior = c
    end
    return maior
end;

perfeitos(1+x)